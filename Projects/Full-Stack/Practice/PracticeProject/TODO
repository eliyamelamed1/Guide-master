Bug-Fixes
    0. after signing in at auth/jwt/create/ url the user is still not authenticated!!! (backend)

backend:
    1. Set users id to uuid at accounts/models.py
    2. Set up email settings at config/settings.py
    
front-end:
    1. if the page refresh after logout, the user get logged in
    2. when an authenticated user visit the login page the page renders the guest nav bar for a second
    3. connect recipes app to front-end

Tests:
    create tests